{
  "version": 2,
  "installCommand": "npm install && cd server && npm install && cd ../client && npm install --include=dev",
  "buildCommand": "cd server && npx prisma generate && cd ../client && npm run build",
  "outputDirectory": "client/dist",
  "crons": [
    {
      "path": "/api/cron/scan",
      "schedule": "0 9 * * 1,4"
    }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/api",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api(?:/.*)?$).*)",
      "destination": "/index.html"
    }
  ]
}
